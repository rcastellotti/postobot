on: push
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - run: |
          git branch -d heroku
          git subtree push --prefix postobot origin heroku
